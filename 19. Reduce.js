//REDUCE as the name suggest sum, mul... to reduce the size of array

let arr=[1,2,3,4,5]
const red=(a,b)=>{            
    return a*b                      //it'll yk * 
}
console.log(arr.reduce(red))

let a= Array.from("Divyam"); 
// or Array.from("Divyam");         //direct in browser, no need of var declaration
console.log(a)
